---
tags:
  - context
  - domain
date: 2026-04-24
domain: {{DOMAIN_NAME}}
---

# {{DOMAIN_NAME}} Context

Domain-specific layer on top of the domain-neutral Core protocol. Read by `framer` subagent at Frame stage when this domain matches the decision being made.

## Goals

What this domain is trying to achieve. (1-3 sentences.)

{{PRIMARY_DOMAINS}}

## Constraints

What constraints apply universally to decisions in this domain (vs being decision-specific). E.g., regulatory limits, organizational policies, technical capabilities.

- (constraint 1 — fill in for your domain)
- (constraint 2)
- (constraint 3)

## Recurring Tasks

5-row table mapping the most-frequent task types in this domain to the 6-stage protocol stages. Each row gives `framer`/`ideator`/`validator` a starting point when that task type appears.

| Recurring Task | Frame focus | Diverge skills | Converge skills | Decide format |
|---|---|---|---|---|
{{RECURRING_TASKS}}

(If RECURRING_TASKS was skipped during setup, edit this table manually with rows for your domain.)

## Domain-specific Anti-patterns

Failures specific to this domain that the validator should watch for, beyond CLAUDE.md's general Anti-Patterns. (Optional — leave blank if no domain-specific patterns identified yet.)

- (anti-pattern 1, if any)

## Knowledge base axes most relevant

Which of the 4 research axes (Decision-Making / Problem-Solving / Causal Reasoning / Creativity) are most heavily used by this domain. The KB seed notes are domain-neutral, but a domain may emphasize specific axes.

- Primary: (axis 1, axis 2)
- Secondary: (axis 3)
